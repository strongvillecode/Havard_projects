#include <stdio.h>
#include <cs50.h>

int main(void)
{
    //Get a number from user
    int x = get_long("x: ");
    int y = get_long("y: ");
    
    //divide x by y
    float z =(float) x / (float) y;
    printf("z = " %f \n", z);
    
}